# ESP Anywhere Builder

Private-alpha Home Assistant OS add-on. Open the Ingress panel, configure or generate an Ed25519 key, store a repository-scoped GitHub token, select an ESPHome YAML file, validate it, then build and publish OTA firmware.
